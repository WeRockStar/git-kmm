default_stages: [push]
repos:
  - repo: local
    hooks:
      - id: build
        name: build ios and android
        entry: ./scripts/build.sh
        language: script
        pass_filenames: false
        verbose: true
